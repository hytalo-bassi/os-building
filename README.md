# os-building
A simple project to start with operational system

# Environment

To turn our assembly code to machine code we will use the `nasm` tool

And to test the code, `qemu` tool. 

## Arch
`pacman -S nasm qemu`

## Debian/Ubuntu
`apt install nasm qemu`
